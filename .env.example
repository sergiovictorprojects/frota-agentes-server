# Copie para .env (local) ou configure no painel do Railway. Nunca versione valores reais.

# Obrigatórias
DATABASE_URL=postgres://usuario:senha@host:5432/frota
ANTHROPIC_API_KEY=sk-ant-substitua-por-uma-chave-dedicada
# Mínimo de 16 caracteres; use uma senha aleatória de 24 ou mais.
UI_PASSWORD=troque-por-uma-senha-longa-e-aleatoria
PUBLIC_BASE_URL=https://frota.exemplo.com

# Opcionais (valores padrão mostrados)
PORT=3000
UI_USER=frota
CRON_PROCESSAR_FILA=*/10 * * * *
MONTHLY_BUDGET_USD=50
MAX_DEMANDAS_POR_RUN=3
MODEL_WORK=claude-sonnet-5
MODEL_AUDIT=claude-sonnet-5
STALE_CLAIM_MINUTES=90

# Avisos por e-mail (opcional: sem isto os avisos ficam só no log do serviço).
# O Railway bloqueia SMTP nos planos Free, Trial e Hobby, por isso o envio usa a API HTTPS do Resend.
# NOTIFY_CHANNEL=email
# RESEND_API_KEY=re_substitua-pela-chave-do-provedor
# NOTIFY_EMAIL_TO=voce@exemplo.com
# NOTIFY_EMAIL_FROM=Frota <onboarding@resend.dev>
